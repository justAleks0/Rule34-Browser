<UserControl x:Class="Rule34GalleryApp.Controls.AppToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">
    <UserControl.Resources>
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetName="ToastTransform"
                             Storyboard.TargetProperty="Y"
                             From="-40" To="0" Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ToastRoot"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.18"/>
        </Storyboard>
    </UserControl.Resources>
    <UserControl.RenderTransform>
        <TranslateTransform x:Name="ToastTransform" Y="-40"/>
    </UserControl.RenderTransform>
    <Border x:Name="ToastRoot"
            Opacity="0"
            Margin="16,12,16,0"
            Padding="14,12"
            CornerRadius="10"
            BorderThickness="1"
            MaxWidth="640"
            HorizontalAlignment="Center"
            VerticalAlignment="Top">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock x:Name="ToastTitle" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,4"/>
                <TextBlock x:Name="ToastBody"
                           TextWrapping="Wrap"
                           FontSize="13"
                           LineHeight="20"/>
            </StackPanel>
            <Button Grid.Column="1"
                    Content="✕"
                    Style="{StaticResource SecondaryButton}"
                    Padding="8,4"
                    VerticalAlignment="Top"
                    Click="Dismiss_OnClick"/>
        </Grid>
    </Border>
</UserControl>
