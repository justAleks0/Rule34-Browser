<UserControl x:Class="Rule34GalleryApp.Views.Overlays.DownloadManagerOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">
    <Grid Background="#CC000000">
        <Border Background="{StaticResource SurfaceRaisedBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="20"
                Width="560"
                MaxHeight="640"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Download manager"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource R34GreenBrush}"/>
                        <TextBlock x:Name="DownloadLibraryHint"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   Margin="0,4,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    <Button Content="✕"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Style="{StaticResource SecondaryButton}"
                            Width="32"
                            Height="32"
                            Padding="0"
                            Click="CloseButton_OnClick"/>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Clear finished"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="ClearFinishedButton_OnClick"/>
                    <Button Content="Open download folder"
                            Style="{StaticResource SecondaryButton}"
                            Click="OpenFolderButton_OnClick"/>
                </StackPanel>

                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              MaxHeight="420">
                    <ItemsControl x:Name="JobsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#141414"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="10"
                                        Margin="0,0,0,8">
                                    <StackPanel>
                                        <Grid>
                                            <TextBlock Text="{Binding DisplayPath}"
                                                       FontWeight="SemiBold"
                                                       FontSize="12"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock HorizontalAlignment="Right"
                                                       Foreground="{StaticResource MutedBrush}"
                                                       FontSize="11"
                                                       Text="{Binding StatusText}"/>
                                        </Grid>
                                        <ProgressBar Height="6"
                                                     Margin="0,8,0,4"
                                                     Minimum="0"
                                                     Maximum="100"
                                                     Value="{Binding Progress}"/>
                                        <TextBlock Foreground="{StaticResource SubtleBrush}"
                                                   FontSize="10"
                                                   TextTrimming="CharacterEllipsis"
                                                   Text="{Binding Tags}"/>
                                        <TextBlock Foreground="#c45c5c"
                                                   FontSize="10"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding ErrorMessage}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <Button Content="Cancel"
                                                HorizontalAlignment="Left"
                                                Margin="0,6,0,0"
                                                Padding="8,2"
                                                Tag="{Binding}"
                                                Click="CancelJobButton_OnClick">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Queued">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Downloading">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock x:Name="EmptyJobsText"
                           Grid.Row="2"
                           Text="No downloads yet."
                           Foreground="{StaticResource MutedBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"/>

                <Button Grid.Row="3"
                        Content="Close"
                        Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0"
                        Click="CloseButton_OnClick"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
