<UserControl x:Class="Rule34GalleryApp.Views.Overlays.ThumbnailPickerOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed">
    <Grid Background="#CC000000">
        <Border Background="{StaticResource SurfaceRaisedBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="20"
                Width="520"
                MaxHeight="640"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Thumbnail frame"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource R34GreenBrush}"/>
                        <TextBlock x:Name="FileNameText"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   Margin="0,4,0,0"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <Button Content="✕"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Style="{StaticResource SecondaryButton}"
                            Width="32"
                            Height="32"
                            Padding="0"
                            Click="CloseButton_OnClick"/>
                </Grid>

                <Border Grid.Row="2"
                        Background="#0A0A0A"
                        CornerRadius="8"
                        MinHeight="240"
                        Margin="0,0,0,12">
                    <Image x:Name="PreviewImage"
                           Stretch="Uniform"
                           Margin="8"/>
                </Border>

                <StackPanel Grid.Row="3" Margin="0,0,0,12">
                    <Grid Margin="0,0,0,6">
                        <TextBlock Text="Position in video"
                                   Foreground="{StaticResource SubtleBrush}"
                                   FontSize="12"/>
                        <TextBlock x:Name="PositionLabel"
                                   HorizontalAlignment="Right"
                                   Foreground="{StaticResource R34GreenBrush}"
                                   FontSize="12"/>
                    </Grid>
                    <Slider x:Name="PositionSlider"
                            Minimum="2"
                            Maximum="98"
                            Value="50"
                            TickFrequency="1"
                            IsSnapToTickEnabled="False"
                            ValueChanged="PositionSlider_OnValueChanged"/>
                    <TextBlock Foreground="{StaticResource MutedBrush}"
                               FontSize="11"
                               Margin="0,6,0,0"
                               Text="Default is the middle of the video. Drag to pick a different frame."/>
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Preview"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,8,0"
                            Click="PreviewButton_OnClick"/>
                    <Button Content="Save thumbnail"
                            Style="{StaticResource PrimaryButton}"
                            Click="SaveButton_OnClick"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
